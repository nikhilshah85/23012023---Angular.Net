import { CurrentInvestmentValuePipe } from './current-investment-value.pipe';

describe('CurrentInvestmentValuePipe', () => {
  it('create an instance', () => {
    const pipe = new CurrentInvestmentValuePipe();
    expect(pipe).toBeTruthy();
  });
});
