import { TotalInvestmentPipe } from './total-investment.pipe';

describe('TotalInvestmentPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalInvestmentPipe();
    expect(pipe).toBeTruthy();
  });
});
